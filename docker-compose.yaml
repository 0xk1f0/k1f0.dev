version: '3'
services:
  astro:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8085:8085"
    environment:
      - TZ=Europe/Vienna
    restart: unless-stopped
